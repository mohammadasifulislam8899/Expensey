package com.xentoryx.expensey.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─── PLAYFUL LIGHT MODE TOKENS ────────────────────────────────────────────────
val PlayfulBg = Color(0xFFFAF9F6)            // Soft warm cream off-white background
val PlayfulCardBg = Color(0xFFFFFFFF)        // Clean white card background
val PlayfulInputBg = Color(0xFFF0EFF5)       // Soft light periwinkle/grey input field background
val PlayfulLavender = Color(0xFF7C67E6)      // Rich primary lavender for active buttons & elements
val PlayfulYellow = Color(0xFFE5A91E)        // Deep warm sunny yellow for accents and sketches
val PlayfulPink = Color(0xFFECA3B9)          // Soft rose pink highlight
val PlayfulTextPrimary = Color(0xFF1A1A1E)   // Dark charcoal for high contrast primary text
val PlayfulTextSecondary = Color(0xFF5E5B75) // Muted slate-grey for descriptions & labels
val PlayfulBorder = Color(0xFFDFDEE7)        // Soft light grey outline border

// ─── CRUSH DARK MODE TOKENS ───────────────────────────────────────────────────
val CrushBg = Color(0xFF0F0F12)            // Deep slate-black background (matching screenshot)
val CrushCardBg = Color(0xFF16161A)        // Deep carbon container background
val CrushInputBg = Color(0xFF1E1E22)       // Sleek graphite input background (Apple button outline style)
val CrushLavender = Color(0xFFA594FD)      // Premium energetic Lavender (from Google button)
val CrushYellow = Color(0xFFF6C344)        // Vibrant energy yellow (from squiggle underline & stars)
val CrushPink = Color(0xFFF6B3C8)          // Subtle pastel pink highlight
val CrushTextPrimary = Color(0xFFFFFFFF)   // Crisp high-contrast white text
val CrushTextSecondary = Color(0xFF8E8BAA) // Muted lavender-grey text
val CrushBorder = Color(0xFF282830)        // Clean hairline border line

// ─── DEFAULT BASE MATERIAL TOKENS (For legacy compatibility) ───────────────────
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)